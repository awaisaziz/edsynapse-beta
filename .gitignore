# ── Repo-root ignores (frontend/ has its own .gitignore too) ──────────────────

# Vercel CLI link + pulled config
.vercel

# Environment files (secrets) — never commit these anywhere in the repo
.env
.env.local
.env.*.local
.env.development.local
.env.preview.local
.env.production.local

# Claude Code agent worktrees + local launch config
.claude/worktrees/

# Node
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

.vercel

.env*
